<IfModule mod_rewrite.c>
    RewriteEngine On

    # Igual ao fluxo do php artisan serve: tudo que não for arquivo real vai ao index.php da raiz
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L,QSA]
</IfModule>
